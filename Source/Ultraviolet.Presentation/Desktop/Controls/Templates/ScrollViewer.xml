<?xml version="1.0" encoding="utf-8" ?>
<ComponentTemplate>
  <View>
    <Grid Class="scrollviewer-component" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="{{ComputedColumn0Width}}"/>
        <ColumnDefinition Width="{{ComputedColumn1Width}}"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <Border
        Grid.Row="0"
        Grid.Column="{{ComputedContentColumn}}"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch">

        <Canvas
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">

          <Image Class="scrollviewer-background"
                 Canvas.Left="0"
                 Canvas.Top="0"
                 Canvas.Right="0"
                 Canvas.Bottom="0"/>
          <ScrollContentPresenter Name="PART_ContentPresenter"
                                  Margin="{{ContentMargin}}"
                                  Canvas.Left="0"
                                  Canvas.Top="0"
                                  Canvas.Right="0"
                                  Canvas.Bottom="0"/>

        </Canvas>


      </Border>
      <HScrollBar Name="PART_HScroll"
                  Class="scrollviewer-hscroll"
                  Grid.Column="{{ComputedContentColumn}}"
                  Grid.Row="1"
                  Visibility="{{ComputedHorizontalScrollBarVisibility}}"
                  HorizontalAlignment="Stretch"
                  SmallChange="16"
                  LargeChange="{{ViewportWidth}}"
                  Value="{{HorizontalOffset}}"
                  Minimum="0" 
                  Maximum="{{ScrollableWidth}}"
                  ViewportSize="{{ViewportWidth}}"/>
      <VScrollBar Name="PART_VScroll"
                  Class="scrollviewer-vscroll"
                  Grid.Column="{{ComputedScrollBarColumn}}"
                  Grid.Row="0"
                  Visibility="{{ComputedVerticalScrollBarVisibility}}"
                  VerticalAlignment="Stretch"
                  SmallChange="16"
                  LargeChange="{{ViewportHeight}}"
                  Value="{{VerticalOffset}}"
                  Minimum="0"
                  Maximum="{{ScrollableHeight}}"
                  ViewportSize="{{ViewportHeight}}"/>
      <Rectangle Class="scrollviewer-rect"
                 FillColor="{{Background}}"
                 Grid.Column="{{ComputedScrollBarColumn}}"
                 Grid.Row="1"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Stretch"/>

    </Grid>
  </View>
</ComponentTemplate>