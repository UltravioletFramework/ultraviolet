<?xml version="1.0" encoding="utf-8" ?>
<ComponentTemplate>
  <Grid ID="LayoutRoot" Class="updown-component" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
  
    <TextBox ID="Input" Class="updown-input" KeyPressed="InputKeyPressed"
             Grid.RowSpan="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">{{Value|0.0}}</TextBox>

    <RepeatButton ID="UpButton" Class="updown-button updown-button-up" 
                  Grid.Column="1" Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="Increment"></RepeatButton>
    <RepeatButton ID="DownButton" Class="updown-button updown-button-down" 
                  Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="Decrement"></RepeatButton>

  </Grid>
</ComponentTemplate>