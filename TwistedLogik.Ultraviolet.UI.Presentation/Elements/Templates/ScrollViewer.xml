<?xml version="1.0" encoding="utf-8" ?>
<ComponentTemplate>
  <Grid ID="LayoutRoot" Class="scrollview-component" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <ContentPresenter Grid.Column="0" Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

    <HScrollBar Class="scrollview-hscroll" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Stretch" SmallChange="1" LargeChange="10"
                Value="{{HorizontalOffset}}" Minimum="0" Maximum="{{ScrollableWidth}}" ViewportSize="{{ViewportWidth}}" 
                IsEnabled="{{ComputedHorizontalScrollBarEnabled}}" Visibility="{{ComputedHorizontalScrollBarVisibility}}" VisibilityChanged="HScroll_VisibilityChanged"/>
    <VScrollBar Class="scrollview-vscroll" Grid.Column="1" Grid.Row="0" VerticalAlignment="Stretch" SmallChange="1" LargeChange="10"
                Value="{{VerticalOffset}}" Minimum="0" Maximum="{{ScrollableHeight}}" ViewportSize="{{ViewportHeight}}" 
                IsEnabled="{{ComputedVerticalScrollBarEnabled}}" Visibility="{{ComputedVerticalScrollBarVisibility}}" VisibilityChanged="VScroll_VisibilityChanged"/>

    <Rectangle Class="scrollview-rect" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    
  </Grid>
</ComponentTemplate>